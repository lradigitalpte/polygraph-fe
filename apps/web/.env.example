# Go API (Railway) — no trailing slash
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# Better Auth / Neon Auth (must be reachable from the browser)
NEXT_PUBLIC_NEON_AUTH_URL=https://your-auth-host/api/auth

# Optional marketing health check
NEXT_PUBLIC_SERVER_URL=https://your-api.up.railway.app

# Railway backend CORS: set FRONTEND_URL or CORS_ORIGINS to this same Vercel URL
